# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# Azure SQL — Server FQDN and database name from the portal
AZURE_SQL_SERVER=sql-krkn-dev-dh.database.windows.net
AZURE_SQL_DATABASE=kraken

# Azure Storage Account name (just the name, not the URL)
AZURE_STORAGE_ACCOUNT=stkrkndevdh

# Optional: override the API port locally
PORT=8000
